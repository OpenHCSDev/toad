# Install in a diagnostic environment, alongside the project's own dependencies.
psutil>=6
python-xlib>=0.33; sys_platform == "linux"
Pillow>=10
py-spy>=0.4
textual-dev>=1.8
memray>=1.20; sys_platform == "linux" or sys_platform == "darwin"
pytest>=8
pytest-xdist>=3
